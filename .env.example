# Copy this file to `.env` and fill in your real values.
# `.env` is gitignored and is read at build time by Android (Gradle) and iOS.

# Google Maps SDK API key (Maps SDK for Android + iOS enabled, key restricted).
MAPS_API_KEY=your_google_maps_api_key_here
